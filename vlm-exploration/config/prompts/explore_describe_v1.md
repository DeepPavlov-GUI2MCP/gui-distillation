You are analyzing a desktop screenshot (resolution: 1280x1024).
The GNOME Calculator window is visible. The rest is black background — ignore it.

Focus ONLY on the calculator window. Describe its current state:

1. screen_label: short name (e.g. "Basic Calculator", "Mode Selection Popup", "Advanced Calculator", "Primary Menu")
2. screen_type: one of "calculator", "popup", "menu", "dialog", "settings"
3. buttons: list ALL visible interactive elements INSIDE the calculator window. Include EVERY button, radio button, menu item, toggle you can see. Use the visible label text for each button.
4. description: 1-2 sentences about what functionality is available

Return JSON matching the schema. Be exhaustive in listing buttons.