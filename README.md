# GUI2MCP

- Общая презентация: https://docs.google.com/presentation/d/1i4rEqOfkRQlc__uMU0U3F-6BWZKQNkRXJUfVzyJH_Mg/edit?usp=sharing
- Запись презентации: https://disk.yandex.ru/i/K899QYM9d4sw_Q

## Аннотация

GUI2MCP исследует, как превращать интерфейсы desktop-приложений в структурированные представления, пригодные для навигации, генерации задач и обучения GUI-агентов. Общая идея проекта — построить pipeline от исследования интерфейса к обучающим данным: сначала автоматически собрать UI-граф приложения, затем использовать его как источник синтетических задач и траекторий, а после этого конвертировать траектории в формат SFT для моделей компьютерного использования.

На текущем этапе реализованы и проверены два подхода к построению UI-графов: A11Y-only exploration для LibreOffice Writer и VLM-based exploration по скриншотам для нескольких desktop-приложений. Поверх полученных графов начата генерация synthetic tasks и траекторий с LLM-размышлениями; для LibreOffice Writer получены 1,495 задач и 1,495 траекторий. Также подготовлена линия конвертации данных в формат UI-TARS-1.5 и scaffolding для дальнейшего SFT GUI-агента на augmented trajectories.

## Проблематика

В рамках литературного обзора были выделены пробелы в существующих работах:

- [gap#1] не генерируют обучающие задачи на основе интерфейса. Источники: [Holo3](https://huggingface.co/Hcompany/Holo3-35B-A3B), [EvoCUA](https://arxiv.org/abs/2601.15876), [Mobile-Agent-v3.5](https://arxiv.org/abs/2602.16855), [ComputerRL](https://arxiv.org/abs/2508.14040), [UI-TARS-2](https://arxiv.org/abs/2509.02544), [OpenCUA](https://arxiv.org/abs/2508.09123), [DART-GUI](https://arxiv.org/abs/2509.23866).
- [gap#2] нет количественной оценки объема SFT, достаточного для эффективности RL. Источники: [Holo3](https://huggingface.co/Hcompany/Holo3-35B-A3B), [EvoCUA](https://arxiv.org/abs/2601.15876), [Mobile-Agent-v3.5](https://arxiv.org/abs/2602.16855), [ComputerRL](https://arxiv.org/abs/2508.14040), [UI-TARS-2](https://arxiv.org/abs/2509.02544), [OpenCUA](https://arxiv.org/abs/2508.09123), [DART-GUI](https://arxiv.org/abs/2509.23866).
- [gap#3] проводят эксперименты на мобильных и веб приложениях, но не на десктопных. Источники: [GraphPilot](https://arxiv.org/abs/2601.17418), [KG-RAG](https://arxiv.org/abs/2509.00366), [GUI-explorer](https://arxiv.org/abs/2505.16827), [OS-Genesis](https://arxiv.org/abs/2412.19723).
- [gap#4] подают знания в контекст модели (RAG), а не дообучают (SFT / RL). Источники: [GraphPilot](https://arxiv.org/abs/2601.17418), [KG-RAG](https://arxiv.org/abs/2509.00366), [GUI-explorer](https://arxiv.org/abs/2505.16827), [OS-Genesis](https://arxiv.org/abs/2412.19723).

Компактный анализ работ для gaps #1-2:

| Paper | Train task types |
| --- | --- |
| [Holo3](https://huggingface.co/Hcompany/Holo3-35B-A3B) | не раскрыто |
| [EvoCUA](https://arxiv.org/abs/2601.15876) | OOD tasks |
| [Mobile-Agent-v3.5](https://arxiv.org/abs/2602.16855) | non-UI-driven |
| [ComputerRL](https://arxiv.org/abs/2508.14040) | non-UI-driven |
| [UI-TARS-2](https://arxiv.org/abs/2509.02544) | non-UI-driven |
| [OpenCUA](https://arxiv.org/abs/2508.09123) | OOD tasks |
| [DART-GUI](https://arxiv.org/abs/2509.23866) | test tasks |

Вклад текущей работы:

- методология автогенерации задач и обучения модели на основе интерфейсов десктопных приложений;
- количественная оценка объема SFT, достаточного для эффективного RL (TODO).

## Постановка задачи

### 1. A11Y-only exploration и построение UI-графа LibreOffice Writer (Тетеревлев Р.В.)

1. Проверить несколько приложений OSWorld-verified и выбрать приложение, пригодное для A11Y-only exploration.
2. Построить pipeline, который извлекает из A11Y Tree активную область интерфейса, доступные действия и состояние интерфейса.
3. Разработать exploration policy, которая отделяет macro-действия от micro действий.
4. Построить проверяемый граф GUI-состояний LibreOffice Writer.
5. Сформировать карту состояний поверх графа, где проверенные переходы дополнены видимыми элементами состояния.
6. Зафиксировать ограничения данного подхода и варианты дальнейшего совершенствования.

### 2. VLM-based exploration и построение UI-графов desktop-приложений (Устинов К.)

1. Реализовать pipeline exploration GUI-приложений, где источник данных — скриншот, а не A11Y-дерево.
2. Использовать MLLM-teacher для семантического описания состояний и выбора действий.
3. Реализовать механизм дедупликации состояний по визуально-семантической сигнатуре, чтобы не раздувать граф.
4. Применить pipeline к нескольким приложениям с разной сложностью UI: gnome-calculator, gnome-text-editor, eog, LibreOffice Writer.
5. Сравнить покрытие VLM-подхода с A11Y-инвентарём там, где A11Y доступен.
6. Зафиксировать ограничения подхода и сформулировать задачи на следующую итерацию.

### 3. Генерация синтетических траекторий и SFT в формате UI-TARS-1.5 (Чернов А.)

1. Провести обзор литературы по автогенерации задач, GUI-agent training и использованию интерфейсных знаний для обучения агентов.
2. Сформировать pipeline генерации синтетических задач и траекторий поверх UI-графа desktop-приложения.
3. Преобразовать сгенерированные траектории в формат UI-TARS-1.5.
4. Создать scaffolding для SFT GUI-агента в UI-TARS-формате на основе augmented trajectories.
5. Подготовить основу для оценки качества SFT через OSWorld и отложенную выборку сгенерированных задач.

## Описание технического решения

### 1. A11Y-only exploration и построение UI-графа LibreOffice Writer (Тетеревлев Р.В.)

Источник данных в A11Y-only подходе — AT-SPI / A11Y Tree. Для каждого UI-узла используются его атрибуты: роль, имя, описание, состояние, координаты, путь родителей и положение во вложенной структуре дерева. Скриншоты в этом подходе не являются источником истины для построения графа, а используются только как вспомогательный артефакт для визуальной проверки состояния.

Перед извлечением действий определяется активная область взаимодействия — `active_root`. Это может быть главное окно, открытое меню, подменю, диалог или overlay. Такой шаг нужен, чтобы не смешивать foreground-контекст, например открытое меню, с фоновыми элементами основного окна.

Далее применяется политика классификации действий. Действия делятся на:
- `macro` — действия, которые могут открыть новый UI-контекст и используются для построения графа;
- `micro` — действия внутри уже открытого контекста, которые не выполняются автоматически как переходы графа, но сохраняются как observation layer.

Для каждого состояния считается A11Y-only signature. Она позволяет определить, изменился ли UI-контекст после выполнения действия и нужно ли создавать новый узел графа. Если действие выполнено и привело к новому состоянию, edge получает статус `confirmed`. Если действие не меняет macro-состояние или отложено политикой безопасности, оно не раздувает граф.

Scheduler использует стратегию BFS: сначала обрабатываются переходы из более близких к root состояний, затем более глубокие уровни. Это обеспечивает равномерное покрытие UI и контролируемое расширение exploration.

Поверх `graph.json` была построена `agent_map.json`. Граф хранит проверенные переходы, а agent map добавляет видимые элементы состояния, scoped observed items и state capabilities.

### 2. VLM-based exploration и построение UI-графов desktop-приложений (Устинов К.)

Источник данных — скриншот текущего состояния приложения; A11Y-дерево не используется как ground truth. Поверх скриншота работает MLLM-teacher (основной — `gpt-5.4`, дополнительно тестировались `gpt-5.4-mini`, `gpt-4.1`, локальный `qwen35-27b` через vLLM), который:
1. описывает текущий экран в семантических терминах (label + NL-описание + список элементов);
2. предлагает следующее действие из словаря атомарных действий.

Pipeline собран из шести шагов: **скриншот → описание через VLM → дедуп состояний по сигнатуре → frontier-список непосещённых действий → выполнение действия → запись узла/перехода в граф**. В отличие от A11Y-подхода, лейблы состояний и имена элементов — семантические (`programming_mode_main`, а не хеш), что делает граф напрямую пригодным как контекст для LLM-генератора задач.

Реализованы несколько exploration-стратегий (`random`, `coverage`, `goal-driven`, `hybrid`) и три раннера: `flat` (последовательный обход), `dfs` (с откатом и перепроходом), `task` (целевой прогон под конкретную задачу). Дедупликация состояний — по комбинации dHash скриншота и семантической сигнатуры от teacher-а. Каждый прогон сохраняется как `state_graph.json` с метаданными (модель, число шагов, экраны, переходы, метрики, usage в токенах и долларах).

Код и финальные графы по приложениям лежат в `vlm-exploration/` (артефакты — в `vlm-exploration/artifacts/state_graphs/`).

### 3. Генерация синтетических траекторий и SFT в формате UI-TARS-1.5 (Чернов А.)

Вклад Антона Чернова связан с последним этапом проекта: превращением построенных UI-графов и augmented trajectories в обучающий материал для GUI-агента. Работа опирается на ветку `feature/synthetic-data` в `gui-a11y-exploration` и ветку `feature/sft` в `dart-gui`.

Этапы генерации синтетических траекторий с помощью LLM:

0. Сгенерировать граф состояний приложения.
1. Дополнить граф в кликабельных элементах UI.
   Пример: "нажать кнопку <экспортировать файл>".
2. Сформулировать задания для путей в графе.
   Пример: "экспортируй текущий файл в формате pdf".
3. Дополнить пути в графе размышлениями LLM.
   Пример: "чтобы экспортировать файл мне нужно...".

Результат этого pipeline — набор задач и траекторий, где каждый пример связывает UI-состояния, действия, цель пользователя и рассуждения агента. Следующий слой работы — конвертация этих данных в UI-TARS-1.5 supervised fine-tuning format и подготовка SFT-запуска для GUI-агента.

## Полученные результаты

### 1. A11Y-only exploration и построение UI-графа LibreOffice Writer (Тетеревлев Р.В.)

#### 1.1. Граф GUI-состояний LibreOffice Writer

Для LibreOffice Writer построен воспроизводимый graph exploration checkpoint:

```text
states / nodes: 42
edges: 41
confirmed: 41
pending: 0
max depth: 3
depth counts: 1 / 11 / 27 / 3
```

В рамках текущей safe macro-policy frontier закрыт: все разрешённые policy переходы выполнены и подтверждены.

Важно: это не означает, что автоматически исследован весь LibreOffice Writer. Потенциально опасные или content-changing действия, такие как Save, Print, Exit, OK, Cancel, Help и dialog checkboxes, намеренно не включались в verified graph и сохранялись только как observed/capabilities в agent map.

#### 1.2. Agent-facing карта

Поверх графа была построена agent-facing карта. Она добавляет к проверенным переходам видимые элементы состояния и возможности интерфейса:

```text
items: 381 (словарь “какие элементы вообще найдены”)
state capabilities: 369 (“что можно видеть или использовать в конкретном состоянии”)
```

`agent_map.json` позволяет агенту понимать, какие элементы видны в текущем состоянии, даже если они не являются проверенными безопасными переходами. Например, в меню File могут быть видны Save и Print, но они остаются observed-only, а не verified transitions.

#### 1.3. Golden set и MLLM-оценка

Дополнительно была подготовлена эталонная разметка UI-элементов для оценки локализации их по скриншоту. На ее основе были протестированы MLLM-модели, предсказывающие bounding boxes элементов интерфейса. Для оценки использовалась метрика IoU. Эти эксперименты стали основой второго направления exploration, где используется визуальный подход через скриншоты и мультимодальные модели.

### 2. VLM-based exploration и построение UI-графов desktop-приложений (Устинов К.)

#### 2.1. Покрытие приложений

Pipeline применён к четырём приложениям: gnome-calculator, gnome-text-editor, eog (image viewer), LibreOffice Writer. Для каждого собран воспроизводимый `state_graph.json` с семантическими лейблами состояний и переходами (артефакты в `vlm-exploration/artifacts/state_graphs/`).

#### 2.2. Сравнение с A11Y там, где оно есть

На gnome-calculator A11Y-дамп использовался как внешний знаменатель для honest coverage:

```text
strict name match:        89.5%
с учётом синонимов:       97.4%
против внешнего GT:       84.6%
```

Единственный устойчивый пропуск — запятая. Содержимое combo-box-ов (~100 валют, единицы измерения и т.п.) pipeline не раскрывает: видит сам селектор и упоминает его в описании, но не скроллит внутри открытого списка.

#### 2.3. Метрика и стоимость прогона

Зафиксировано фундаментальное ограничение: без A11Y-дампа или ручной разметки нет знаменателя для абсолютного coverage — «мы не знаем чего мы не знаем». Поэтому используются относительные дельта-метрики: фиксируется приложение и teacher, меняется pipeline, и сравниваются прогоны между собой. Все правки pipeline в ходе работы стабильно двигали относительное покрытие в нужную сторону. Стоимость одного полного прогона укладывается примерно в $5 на teacher-вызовы.

### 3. Генерация синтетических траекторий и SFT в формате UI-TARS-1.5 (Чернов А.)

Для графа LibreOffice Writer получены:

- 231 кликабельных меню в исходном графе;
- 1,495 заданий, то есть 6.47 заданий на кликабельное меню;
- 1,495 траекторий выполнения заданий с размышлениями.

Также подготовлена линия работы по конвертации датасета в UI-TARS-1.5 format и scaffolding для SFT на augmented trajectories. Это связывает exploration-часть проекта с обучением GUI-агента и дальнейшими RL-экспериментами.

Протоколы оценивания качества SFT:

1. Задачи OSWorld для соответствующего приложения (TODO).
2. Отложенная выборка сгенерированных заданий (TODO).

## Практическая значимость

### 1. A11Y-only exploration и построение UI-графа LibreOffice Writer (Тетеревлев Р.В.)

Полученный A11Y-only граф может использоваться как:
- источник проверенных навигационных переходов по интерфейсу;
- основа для генерации обучающих траекторий;
- база для построения SFT-датасета;
- промежуточное представление интерфейса для Computer-use агента;
- потенциальный MCP-tool или навигационная память для LLM-агента.

В рамках командной работы результаты были использованы для генерации обучающих примеров (сформированы задачи для SFT).

### 2. VLM-based exploration и построение UI-графов desktop-приложений (Устинов К.)

Подход даёт самостоятельный baseline там, где A11Y-подход неприменим или нестабилен:
- платформы без A11Y — Windows, Electron-обёртки, проприетарные UI;
- приложения, где A11Y нестабилен — Impress, VLC, Thunderbird (по результатам скрининга);
- ситуации, когда нужна семантика поверх A11Y — NL-имена и описания, готовые для LLM.

Полученные `state_graph.json` уже сейчас пригодны как контекст для LLM-генератора задач и как baseline для первой версии task-generator-а. В рамках общего проекта это направление дополняет A11Y-only подход и расширяет множество приложений, к которым применимо построение UI-графа.

**Вывод.** PoC успешный: семантический граф собирается, pipeline воспроизводим, базовая стоимость прогона приемлема. Главное, над чем предстоит работать дальше — устойчивая метрика покрытия и дебаг-инструменты; без них подход остаётся PoC-ом. В то же время уже на текущем этапе подход применим там, где A11Y-дерева нет или оно нестабильно.

### 3. Генерация синтетических траекторий и SFT в формате UI-TARS-1.5 (Чернов А.)

Работа делает UI-граф не только артефактом exploration, но и источником обучающих данных. Сгенерированные задачи и траектории позволяют масштабировать SFT без ручной разметки каждого сценария, а формат UI-TARS-1.5 делает данные совместимыми с существующей линией GUI-agent training.

Практический результат — связка "UI-граф → synthetic tasks → trajectories with reasoning → UI-TARS SFT dataset → GUI-agent SFT scaffolding".

## Ограничения

### 1. A11Y-only exploration и построение UI-графа LibreOffice Writer (Тетеревлев Р.В.)

У A11Y-only подхода есть ограничения масштабирования:

1. Не все приложения стабильно отдают A11Y Tree.
2. Структура дерева может сильно отличаться между приложениями.
3. Для разных классов UI может требоваться адаптация exploration policy.
4. A11Y Tree не всегда достаточно хорошо отражает визуальные или canvas-зависимые интерфейсы.

Поэтому A11Y-only exploration целесообразно рассматривать как один из компонентов общего GUI2MCP pipeline, дополняемый визуальным exploration через MLLM.

### 2. VLM-based exploration и построение UI-графов desktop-приложений (Устинов К.)

Ограничения текущего состояния VLM-pipeline:

1. Нет устойчивой методики абсолютного coverage на сложных приложениях (Writer и далее) — нет знаменателя.
2. Нет надёжного матчинга VLM↔A11Y имён: синонимы и разный нейминг.
3. Pipeline не раскрывает содержимое прокручиваемых dropdown-ов (combo-box-ы со списками валют, единиц и т.п.).
4. Нет per-state A11Y-снимков для автоматической проверки галлюцинаций teacher-а.
5. Дебаг-инструменты и воспроизводимость прогонов ещё сырые.
6. Метрики прогона — относительные, не абсолютные; корректно сравнивают стратегии при одной модели, но не дают честного «нашли X% приложения».

### 3. Генерация синтетических траекторий и SFT в формате UI-TARS-1.5 (Чернов А.)

Ограничения текущего состояния:

1. Качество синтетических задач зависит от полноты UI-графа и точности LLM-генерации.
2. Протоколы оценки SFT пока требуют реализации и валидации.
3. Количественная оценка достаточного объема SFT для эффективного RL остается открытым экспериментальным вопросом.

## Дальнейшие планы

### 1. A11Y-only exploration и построение UI-графа LibreOffice Writer (Тетеревлев Р.В.)

Дальнейшее развитие может идти в нескольких направлениях:

1. Расширить проверку A11Y-only подхода на другие приложения и классы интерфейсов.
2. Улучшить автоматическую адаптацию политики exploration под разные типы A11Y Tree.
3. Сжать граф в более информативное представление: например, векторную или графовую базу знаний.
4. Использовать граф и agent map как MCP-tool для LLM-агента, а не только как источник обучающих данных.

### 2. VLM-based exploration и построение UI-графов desktop-приложений (Устинов К.)

1. Разработать устойчивую методику измерения coverage без A11Y — через ручную разметку или гибрид с A11Y там, где он доступен.
2. Реализовать надёжный матчинг VLM↔A11Y имён для honest-coverage по более широкому набору приложений.
3. Расширить pipeline на работу с прокручиваемыми dropdown-ами и длинными списками.
4. Добавить per-state A11Y-снимки как механизм автоматической верификации описаний teacher-а.
5. Доразвить дебаг-инструменты и воспроизводимость прогонов.
6. Использовать собранные `state_graph.json` как источник трасс для следующего этапа — обучения exploration-политики методами RL.

### 3. Генерация синтетических траекторий и SFT в формате UI-TARS-1.5 (Чернов А.)

1. Завершить протокол оценки SFT на OSWorld-задачах для соответствующих приложений.
2. Подготовить отложенную выборку сгенерированных задач для проверки generalization.
3. Провести серию SFT-экспериментов с разным объемом синтетических траекторий.
4. Оценить, какой объем SFT достаточен для более эффективного RL.

## Команда и распределение задач

### 1. A11Y-only exploration и построение UI-графа LibreOffice Writer (Тетеревлев Р.В.)
Зона ответственности:
- скрининг OSWorld-приложений на пригодность для A11Y-only exploration;
- исследование и настройка A11Y Tree capture через AT-SPI;
- разработка и отладка A11Y-only exploration pipeline;
- формирование политики разделения macro- и micro-действий;
- построение графа состояний LibreOffice Writer;
- построение agent-facing карты поверх графа;
- подготовка визуализаций graph / agent map;
- участие в подготовке golden set разметки GUI-элементов;
- участие в тестировании MLLM-моделей для локализации UI-элементов через IoU.

Связь с работой команды:
- результаты A11Y-only графа и agent map были переданы для генерации обучающих задач и траекторий SFT;
- golden set и IoU-оценка MLLM-моделей были использованы в направлении exploration через скриншоты;
- работа дополняет общий GUI2MCP exploration pipeline.

### 2. VLM-based exploration и построение UI-графов desktop-приложений (Устинов К.)
Зона ответственности:
- разработка screenshot-based exploration pipeline без опоры на A11Y;
- интеграция MLLM-teacher (gpt-5.4 / gpt-5.4-mini / gpt-4.1 / qwen35-27b через vLLM) для семантического описания состояний и выбора действий;
- реализация exploration runners: `flat`, `dfs`, `task`;
- реализация exploration-стратегий: `random`, `coverage`, `goal-driven`, `hybrid`;
- дедупликация состояний (dHash скриншота + семантическая сигнатура);
- применение pipeline к gnome-calculator, gnome-text-editor, eog, LibreOffice Writer;
- сравнение покрытия VLM-подхода с A11Y-инвентарём на gnome-calculator;
- метрики прогонов (число экранов / переходов / стоимость в токенах и долларах) и формат `state_graph.json`.

Связь с работой команды:
- VLM-pipeline дополняет A11Y-подход и расширяет множество приложений, к которым применимо построение UI-графа;
- собранные прогоны exploration — материал для следующего этапа проекта (RL по exploration policy).

### 3. Генерация синтетических траекторий и SFT в формате UI-TARS-1.5 (Чернов А.)
Зона ответственности:
- обзор литературы по synthetic task generation, GUI-agent training и интерфейсным знаниям для обучения агентов;
- формулировка gaps существующих работ и вклада текущего подхода;
- генерация synthetic tasks поверх UI-графа LibreOffice Writer;
- генерация траекторий выполнения заданий с размышлениями LLM;
- конвертация датасета в формат UI-TARS-1.5;
- scaffolding для SFT GUI-агента на augmented trajectories;
- подготовка протоколов оценки SFT через OSWorld и отложенную выборку задач.

Связь с работой команды:
- использует A11Y/VLM-графы как источник обучающих задач;
- связывает exploration pipeline с supervised fine-tuning GUI-агента;
- готовит основу для дальнейшей количественной оценки влияния SFT на эффективность RL.
